{
  "name": "seed-static-site",
  "version": "0.0.1",
  "description": "Seed project for a static site.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "private": "true",
  "devDependencies": {
    "serve-static": "^1.12.4"
  }
}
